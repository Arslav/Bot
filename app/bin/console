#!/usr/bin/env php
<?php

use Arslav\Newbot\Cli;
use Psr\Container\ContainerInterface;

/** @var ContainerInterface $container */
$container = require __DIR__ . '/../bootstrap.php';

$app = new Cli($container, $argv ?? []);
$app->run();